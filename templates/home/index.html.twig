{% extends 'base.html.twig' %}

{% block title %}Hello HomeControllerde l'index !{% endblock %}

{% block body %}


// défition de class dans le fichier twig
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

//Cotenu 
<div class="example-wrapper">
    <h1>Hello {{ truc }}! ✅</h1> 


{% if user.isConnected == TRUE %}
<button>Déconnexion</button>
{% else %}
<button>Connexion</button>
{% endif %}



    <article>
        <H2> Titre : {{article.title}}</H2>
        <p> Intro : {{article.intro}}</p>
        <p> Contenu :  {{article.content}}</p>
        <cite>Auteur : {{auteur}}</cite>
    </article>

    <p> truc simple : {{truc}}</p>
    <p> truc title :{{truc|title}}</p>
    <p> truc title :{{truc|upper}}</p>


{% if true %}
    <p>Hello coco</p>
{% else %}
    <p>by</p>
{% endif %}


{% endblock %}


{% block alerte %}
{% endblock %}
