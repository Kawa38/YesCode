        <div class="col-4">
            <div class="card bg-light my-5 shadow">
                <h3 class="card-header text-center">{{ article.title}}</h3>
                <img height="350px"  src={{article.image}} alt="Card image">
                <div class="card-body flex-d">
                  <p class="card-text text-center">{{ article.intro}}</p>
                  <a href="#" class="btn btn-primary rounded m-auto">Lire la suite</a>
                  <div class="my-3">
                      <a href="#" class="card-link">{{ article.author}}</a>
                  </div>
                </div>
                <div class="card-footer text-muted">
                  Il y a 2 jours
                </div>
            </div>
        </div>
